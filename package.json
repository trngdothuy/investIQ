{
  "name": "investIQ",
  "version": "0.1.0",
  "private": true,
  "description": "investIQ — MigraCode fullstack project",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "lint": "oxlint frontend/src backend/src",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "test": "npm run test --prefix frontend && npm run test --prefix backend"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "oxfmt": "^0.54.0",
    "oxlint": "^1.69.0"
  },
  "volta": {
    "node": "24.16.0",
    "npm": "11.13.0"
  },
  "dependencies": {
    "daisyui": "^5.5.23"
  }
}
